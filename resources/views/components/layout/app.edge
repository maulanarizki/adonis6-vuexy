<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title || "Adonis DDD" }}</title>

  @if ($slots.meta)
    {{{ await $slots.meta() }}}
  @endif

  @vite(['resources/js/app.js', 'resources/css/app.css'])
</head>
<body>
  <header>
    <nav>
      <a href="{{ route('home') }}">Home</a>
      <a href="{{ route('auth.login.show') }}">Login</a>
    </nav>
  </header>

  <main>
    {{{ await $slots.main() }}}
  </main>
</body>
</html>
