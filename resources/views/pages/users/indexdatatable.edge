@layout.admin()

    @slot('description')
    <title>Daftar User | Your Company Name</title>
    {{-- Tambahkan CSS untuk DataTables --}}
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
    @endslot

    @slot('main')
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card shadow-sm rounded-4 border-0">
                        <div class="card-body">
                            <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center mb-3 gap-2">
                                <h4 class="fw-bold mb-0">Data Pengguna</h4>
                                <a href="{{ route('users.create') }}" class="btn btn-primary">
                                    <i class="fa fa-plus me-1"></i> Tambah Pengguna
                                </a>
                            </div>

                            <table id="users-table" class="table table-bordered table-hover" style="width:100%">
                                <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Full Name</th>
                                    <th>Username</th>
                                    <th>Email</th>
                                    <th>Dibuat</th>
                                    <th>Aksi</th>
                                </tr>
                                </thead>
                                <tbody>
                                  {{-- Body akan diisi oleh DataTables --}}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    @endslot


    @slot('scripts')
    {{-- Tambahkan JS untuk jQuery dan DataTables --}}
    <script src="https://code.jquery.com/jquery-3.7.0.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>
    <script>
    $(document).ready(function() {
      $('#users-table').DataTable({
          processing: true,
          serverSide: true,
          ajax: "{{ route('users.data') }}",
          columns: [
              { data: 'id', name: 'id' },
              { data: 'fullName', name: 'full_name' },
              { data: 'username', name: 'username' },
              { data: 'email', name: 'email' },
              { 
                data: 'createdAt', 
                name: 'created_at',
                render: function(data, type, row) {
                  // Format tanggal jika diperlukan
                  return new Date(data).toLocaleDateString();
                }
              },
              {
                  data: 'id',
                  orderable: false,
                  searchable: false,
                  render: function(data, type, row) {
                      const showUrl = "{{ route('users.show', { id: ':id' }) }}".replace(':id', data);
                      const editUrl = "{{ route('users.edit', { id: ':id' }) }}".replace(':id', data);
                      const deleteUrl = "{{ route('users.destroy', { id: ':id' }) }}".replace(':id', data);

                      return `
                          <a href="${showUrl}" class="btn btn-info btn-sm">Lihat</a>
                          <a href="${editUrl}" class="btn btn-warning btn-sm">Edit</a>
                          <form action="${deleteUrl}?_method=DELETE" method="POST" style="display:inline" onsubmit="return confirm('Yakin hapus user?');">
                            {{ csrfField() }}
                            <button type="submit" class="btn btn-danger btn-sm">Hapus</button>
                          </form>
                      `;
                  }
              }
          ]
      });
    });
    </script>
    @endslot

@end